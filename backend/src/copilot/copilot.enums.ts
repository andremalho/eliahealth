export enum AlertType {
  PATTERN_DETECTED = 'pattern_detected',
  EXAM_OVERDUE = 'exam_overdue',
  RED_FLAG = 'red_flag',
  SUGGESTION = 'suggestion',
  RISK_INCREASE = 'risk_increase',
}

export enum AlertSeverity {
  INFO = 'info',
  WARNING = 'warning',
  URGENT = 'urgent',
  CRITICAL = 'critical',
}
